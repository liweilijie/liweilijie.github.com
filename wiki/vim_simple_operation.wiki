%toc vim简单的运算
%title vim简单的运算
%template vim

在插入模式下输入 `C-r` then `=`  即可输入数学表达式运算

其实是个简单的小功能，就是实现简单的数值计算，不过却可以省去你离开Vim去打开计算器的时间。 

*操作方式如下：*

插入模式下输入 `ctrl+r` ，然后输入 `=` ，输入任意算数式，比如 `129*129` ，回车，即可看到结果。

